<div class="max-w-xl mx-auto px-4">
  <h1 class="mb-4 block max-w-xl mx-auto text-lg font-semibold ">料理記録の一覧</h1>
  <%= link_to 'ホームに戻る', dashboard_path, class: "mb-5 py-3 block w-full max-w-xl mx-auto flex items-center justify-center rounded-md font-bold text-white bg-[#212121]" %>
  <p class="mb-4">☑️ これまでの記録<%= @cooking_records.count %>件</p>
  <div class="mb-4 block w-full max-w-xl mx-auto space-y-4">
    <% if @cooking_records.present? %>
      <% @cooking_records.each do |cooking_record| %>
        <%= link_to cooking_record_path(cooking_record), class: "p-4 rounded-md bg-gray-100 block" do %>
          <% if cooking_record.photo.attached? %>
            <%= image_tag cooking_record.photo, class: "mb-4 rounded-md object-cover"%>
          <% else %>
            <div class="w-full aspect-[4/3] rounded-md bg-gray-100 mb-4">
              <div class="h-full w-full rounded-md flex items-center justify-center bg-white">
                <svg class="w-10 h-10 text-gray-400" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                    aria-hidden="true">
                  <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
                  <circle cx="8.5" cy="8.5" r="1.5"></circle>
                  <polyline points="21 15 16 10 5 21"></polyline>
                </svg>
              </div>
            </div>
          <% end %>
          <p>
            <%= cooking_record.cooked_on.strftime("%Y/%m/%d")%><br>
            <%= cooking_record.dish_name %>
          </p>
        <% end %>
      <% end %>
    <% else %>
      <p class="mb-4">記録がありません</div>
    <% end %>
  </div>
</div>
