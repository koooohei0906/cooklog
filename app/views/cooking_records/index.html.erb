<div class="max-w-xl mx-auto px-4">
  <h1 class="mb-4 block max-w-xl mx-auto text-lg font-semibold ">料理記録の一覧</h1>
  <%= link_to 'ホームに戻る', dashboard_path, class: "mb-5 py-3 block w-full max-w-xl mx-auto flex items-center justify-center rounded-md font-bold text-white bg-[#212121]" %>
  <p class="mb-4">☑️ これまでの記録<%= @cooking_records.count %>件</p>
  <div class="mb-4 w-full max-w-xl mx-auto">
    <% if @cooking_records.present? %>   
      <div class="grid grid-cols-3 gap-2 border"><!-- TODO: ボーダー後から消す -->
        <% @cooking_records.each do |cooking_record| %>
          <%= link_to cooking_record_path(cooking_record), class: "block border" do %><!-- TODO: ボーダー後から消す -->
            <div class="relative">
              <!-- 日付オーバーレイ -->
              <div class="absolute top-1 left-1 z-10 rounded-md bg-white/80 px-1.5 py-0.5 text-sm font-semibold text-gray-900">
                <%= cooking_record.cooked_on.strftime("%m/%d") %>
              </div>
              <!-- サムネ -->
              <% if cooking_record.photo.attached? %>
                <%= image_tag cooking_record.photo, class: "w-full aspect-square rounded-md object-cover"%>
              <% else %>
                <div class="w-full aspect-square rounded-md bg-white flex items-center justify-center">
                  <svg class="w-8 h-8 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
                    <circle cx="8.5" cy="8.5" r="1.5"></circle>
                    <polyline points="21 15 16 10 5 21"></polyline>
                  </svg>
                </div>
              <% end %>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <p class="mb-4">記録がありません</div>
    <% end %>
  </div>
</div>
